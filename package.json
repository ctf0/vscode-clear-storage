{
    "name": "clear-storage",
    "displayName": "Clear Storage",
    "description": "clear workspace & global storage",
    "version": "0.0.3",
    "publisher": "ctf0",
    "repository": "https://github.com/ctf0/vscode-clear-storage",
    "engines": {
        "vscode": "^1.110.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onStartupFinished"
    ],
    "main": "./out/main",
    "contributes": {
        "commands": [
            {
                "command": "extension.clearWorkSpaceStorage",
                "title": "Clear Storage: WorkSpaceStorage"
            },
            {
                "command": "extension.clearGlobalStorage",
                "title": "Clear Storage: GlobalStorage"
            },
            {
                "command": "extension.openWorkSpaceStorage",
                "title": "Clear Storage: Open WorkSpaceStorage"
            },
            {
                "command": "extension.openGlobalStorage",
                "title": "Clear Storage: Open GlobalStorage"
            }
        ]
    },
    "scripts": {
        "vscode:prepublish": "npm run esbuild-base -- --pure:console.log --minify",
        "esbuild-base": "esbuild ./src/extension.ts --bundle --outfile=out/main.js --external:vscode --external:fs-extra --format=cjs --platform=node",
        "esbuild": "npm run esbuild-base -- --sourcemap",
        "esbuild-watch": "npm run esbuild-base -- --sourcemap --watch",
        "watch": "npm run esbuild"
    },
    "devDependencies": {
        "@types/node": "^26.1.2",
        "@types/vscode": "^1.110.0",
        "esbuild": "^0.28.1",
        "typescript": "^7.0.2"
    },
    "dependencies": {
        "fs-extra": "^11.4.0"
    }
}
